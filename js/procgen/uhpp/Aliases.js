import { WFCManager } from './WFCManager.js';
import { UniversalPipeline } from './UniversalPipeline.js';
import { CellularAutomata } from './CellularAutomata.js';

/**
 * @module UHPP/Aliases
 * @description Provides aliases for the core UHPP classes to match requested output names.
 */

export const TileSolver = WFCManager;
export const GridSolver = UniversalPipeline;
export const CAGridSolver = CellularAutomata;
